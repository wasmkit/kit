/**
 * This module format does nothing and can be used as a template
 */

import { AbstractFormat } from "../abstract";


export class NopFormat extends AbstractFormat {
    public extract(): void {
        
    }

    public compile(): void {
        
    }
}
